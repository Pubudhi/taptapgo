# discovery-service configurations for config-server
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: htp://${eureka.instance.hostname}/${server.port}/eureka/

server:
  port: 8761